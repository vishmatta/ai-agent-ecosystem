# Dependencies
node_modules

# Quartz build output and caches
public
.quartz-cache
.quartz/
quartz/cli/tui/dist/
tsconfig.tsbuildinfo
prof
.turbo/

# Secrets and local environment — never commit
.env
.env.*
!.env.example
*.pem
*.key

# Editor and OS cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~

# Logs
quartz-build.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Personal workspaces
.obsidian
private/
.replit
replit.nix

# Claude Code: per-machine state. launch.json (the dev-server config) stays
# tracked; git worktrees and personal settings do not.
.claude/worktrees/
.claude/settings.local.json
