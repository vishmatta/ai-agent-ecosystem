---
name: content-idea
description: Turn a content idea for the AI Agent Ecosystem site (a new Landscape entry, concept, correction, category, or section) into the right GitHub issue, following this repository's intake stage. Use when the user has an idea for the site, wants the site to add or cover something, or asks to process an issue labelled "idea". It checks, routes, and triages the idea; it does not start the work.
metadata:
  version: "1.0"
---

# Content idea to issue

This skill is a shortcut. The procedure lives in the repository, so every tool
follows the same one.

1. From the repository root, read `docs/agents/intake.md` and follow it
   exactly.
2. The idea is whatever the user wrote with this request, or the body of the
   `idea` issue they named. If there's neither, ask for the idea in one
   sentence.
3. Stop where intake stops. Report the issue you created or updated, its
   labels, and why. Don't start the work.

If this file and `docs/agents/intake.md` ever disagree, `docs/agents/intake.md`
wins.
